DROP TABLE IF EXISTS discs;

CREATE TABLE discs (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NOT NULL,
  brand TEXT NOT NULL,
  speed NUMERIC(3, 1) NOT NULL,
  glide NUMERIC(2, 1) NOT NULL,
  turn NUMERIC(2, 1) NOT NULL,
  fade NUMERIC(2, 1) NOT NULL
);

DROP TABLE IF EXISTS users;

CREATE TABLE users (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  first_name TEXT NOT NULL,
  last_name TEXT NOT NULL,
  username TEXT NOT NULL,
  email TEXT NOT NULL,
  password TEXT NOT NULL
);

DROP TABLE IF EXISTS user_bag_discs;

CREATE TABLE user_bag_discs (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER REFERENCES users(id) NOT NULL,
  disc_id INTEGER REFERENCES discs(id) NOT NULL
);

DROP TABLE IF EXISTS scorecards;


-- include date information for scorecards
CREATE TABLE scorecards (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER REFERENCES users(id) NOT NULL,
  hole_1 INTEGER DEFAULT 0,
  hole_2 INTEGER DEFAULT 0,
  hole_3 INTEGER DEFAULT 0,
  hole_4 INTEGER DEFAULT 0,
  hole_5 INTEGER DEFAULT 0,
  hole_6 INTEGER DEFAULT 0,
  hole_7 INTEGER DEFAULT 0,
  hole_8 INTEGER DEFAULT 0,
  hole_9 INTEGER DEFAULT 0,
  hole_10 INTEGER DEFAULT 0,
  hole_11 INTEGER DEFAULT 0,
  hole_12 INTEGER DEFAULT 0,
  hole_13 INTEGER DEFAULT 0,
  hole_14 INTEGER DEFAULT 0,
  hole_15 INTEGER DEFAULT 0,
  hole_16 INTEGER DEFAULT 0,
  hole_17 INTEGER DEFAULT 0,
  hole_18 INTEGER DEFAULT 0
);